1) IP addresses within a network:
nmap -sn 192.168.8.0/24

2) Open a certain serial port
sudo minicom -D /devttyUSB0

3) Copy files from one computer to another
scp ./file.a nestor@192.168.8.205:/home/nestor

4) Install DPKg files
sudo dpkg -i ./file

5) Switch off a computer immediately
sudo shutdown -h now

6) Where all the flash drivers and hard disks stored on Linux?
/media/nestor/

7) Check the content of HEX or binary files. -b -c -C -d -e -f -L -n -o -s -v -x
hexdump -C filename

8) Check the type of file by typing 
file file_name

9) Make file executable
chmod +x filename

10) sudo apt remove --purge xxsoftware

11) sudo apt autoremove

12) Install compiler for ARM64 bit architecture

sudo apt install gcc-aarch64-linux-gnu

13) Which bindings are important when writing device tree? 
Documentation/devicetree/bindings

14) cat /proc/interrupts
Checking interrupts, which core handled and how many times

15) Check what version of Linux is running on a certain processor
uname -r or -m or -a

16) How to search recursively a directory and its subdirectories
find ./ -name "Hello" where Hello is the name of the file. ./ is the directory

17) How to extract files from file.tar.gz
tar -xf file.tar.gz

18) How to add certain properties to a file
chmod +x file to add executability
chmod -x file to remove executability

19) How to know the size of a directory or a file
du -h

21) How to know the size of disks and everything
df -h

22) How to edit a crontab
crontab -e

23) How to list all crontab
crontab -l22) How to edit a crontab
crontab -e

24) How to see assembly instuctions of a certain executable file.
objdump -d /usb/sbin/i2cdetect

25) How to know what version of Linux GLIB you are using 
ldd -v or ldd --version

26) Copy files from one PC to another using scp
scp filename pi@192.168.8.254:/home/pi

27) How to copy files remote or locally using rsync
rsync source destination
rsync -avz pi@192.168.8.254:/home/pi/ destination

28) How to know the size of a directory 
du -h
du -h --summarize
du -h --max-depth=0

29) Disable all password prompts
- sudo visudo
- Add line: nestor ALL=(ALL) NOPASSWD: ALL where nestor is the username

30) SSH into a remote session without a password
ssh-keygen -t ed25519 -C "debian@192.168.8.168"
ssh-copy-id debian@192.168.8.198

31) View what a user can do
sudo -l

32) Know the occurence or where a certain function is defined. 
rg -n J722S_MCU_IOPAD 
The command can be found at sudo apt install ripgrep

33) How to detect devices that connected to i2c bus
i2cdetect -y 1

34) List all I2C buses
i2cdetect -l

35) How minicom data into a file
sudo minicom -C /home/nestor/serial.log -D /dev/ttyUSB0

36) Set the password for root
sudo passwd root

37) Login as root
su - or 
sudo -i

38) How to change the password for a particular user
sudo passwd debian where debian is the username

39) Scan a i2c bus
sudo i2cdetect -y -r 1

